% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/castxml.R
\name{locate_castxml}
\alias{locate_castxml}
\title{Locate a CastXML executable}
\usage{
locate_castxml(path = getOption("porter.castxml"))
}
\arguments{
\item{path}{Optional path to either the CastXML executable or a directory
containing it. Defaults to \code{getOption("porter.castxml")}.}
}
\value{
A single named character vector. The value is the normalized path to
the CastXML executable, and the name is the detected version. Returns \code{NULL}
if no usable CastXML executable is found.
}
\description{
\code{locate_castxml()} finds a CastXML executable installed by the user. porter
does not download or install CastXML; install it with your system package
manager before calling \code{\link[=port]{port()}}.
}
\details{
When \code{path} is supplied, only that executable or directory is checked. With
the default \code{NULL} path, the search order is:
\itemize{
\item \code{Sys.which("castxml")}.
\item Common package-manager locations that may not be on \code{PATH}, including
Homebrew, Linuxbrew, Scoop, Chocolatey, and conda prefixes.
}

Package-manager installations are usually found by \code{Sys.which("castxml")}
when their shims or binary directories are on \code{PATH}.
}
\examples{
\dontshow{if (!is.null(porter::locate_castxml())) withAutoprint(\{ # examplesIf}
locate_castxml()
\dontshow{\}) # examplesIf}
}
