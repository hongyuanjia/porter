% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/port.R
\name{port_report}
\alias{port_report}
\title{Inspect diagnostics recorded while generating a dynport}
\usage{
port_report(port, kind = NULL)
}
\arguments{
\item{port}{A \code{dynport} object created by \code{\link[=port]{port()}}.}

\item{kind}{Optional diagnostic kind to filter by.}
}
\value{
A data frame with diagnostic entries, including source file and line
when available.
}
\description{
Inspect diagnostics recorded while generating a dynport
}
\examples{
\dontshow{if (!is.null(porter::locate_castxml())) withAutoprint(\{ # examplesIf}
header <- tempfile(fileext = ".h")
writeLines(c(
    "#define ADD_ONE(x) ((x) + 1)",
    "int add(int a, int b);"
), header)

p <- port(header)
port_report(p)
port_report(p, "unsupported_macro")
\dontshow{\}) # examplesIf}
}
